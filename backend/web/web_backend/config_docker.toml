# Public host of login server
public_host = ""
port = 25674

# SSL Configuration for RusTLS
certificate_file = "res/cert.pem"
key_file = "res/key.pem"

login_provider = "http:/dev_login_provider:9000"
user_provider = "http://dev_user_provider:8000"

[database]
host = "database"
port = 5432
user = "postgres"
database = "postgres"
password = "1234"
